# GAZİ Kampüs Uçuş Simülatörü — köprü bağımlılıkları
# Kurulum: baslat.sh / baslat.bat otomatik yapar; elle:
#   python3 -m venv .venv
#   .venv/bin/pip install -r kopruler/requirements.txt        (Linux)
#   .venv\Scripts\pip install -r kopruler\requirements.txt    (Windows)
pyserial>=3.5
websockets>=12,<16
bleak>=0.22
